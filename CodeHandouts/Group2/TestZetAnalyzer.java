public class TestZetAnalyzer
{
    public static void main (String args [])
    {
        ZetDeck deck=new ZetDeck();
        /*ZetTable table=new ZetTable();
         * for (int i=0; i<table.length; i++)
        {
            for (int x=i+1; x<table.length; x++)
            {
                for (int y=x+1; y<table.length; y++)
                {
                    if (isZet(table[i],table[x],table[y])==true)
                    {
                        System.out.println("Set: "+table[i]+ ", "+table[x]+ ", "+table[y]);
                    }
                }
            }
        }
         * System.out.println(ZetAnalyzer.findZet(table));
         * 
         */
    }
}